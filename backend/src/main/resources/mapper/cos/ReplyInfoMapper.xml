<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.mrbird.febs.cos.dao.ReplyInfoMapper">

    <!-- 分页获取消息回复信息 -->
    <select id="selectReplyPage" resultType="java.util.LinkedHashMap">
        select
        ri.id,
        ri.tag_id,
        ri.user_id,
        ri.content,
        ri.post_id,
        ri.reply_user_id,
        ri.send_create,
        ri.delete_flag,
        si.name as studentName,
        si.images as studentImages,
        poi.title
        from
        reply_info ri
        left join student_info si on
        (si.id = ri.user_id)
        left join post_info poi on
        (poi.id = ri.post_id)
        where
        1 = 1
        <if test="replyInfo.studentName != null and replyInfo.studentName != ''">
            AND si.name LIKE CONCAT('%',#{replyInfo.studentName},'%')
        </if>
        <if test="replyInfo.title != null and replyInfo.title != ''">
            AND poi.title LIKE CONCAT('%',#{replyInfo.title},'%')
        </if>
        order by
        send_create desc
    </select>
</mapper>
